# Tells npm to use legacy peer dependency resolution (npm 6 behaviour).
# Required because @optimizely/cms-cli has an optional peerDep on React 19
# while this project uses React 18. The conflict is benign — the CLI is a
# dev-only tool and never runs in the browser or on the server.
legacy-peer-deps=true
